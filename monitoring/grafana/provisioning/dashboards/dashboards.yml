apiVersion: 1

providers:
  - name: 'Default Dashboards'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      # Dashboard JSON dosyalarının okunacağı dizin
      path: /etc/grafana/provisioning/dashboards
