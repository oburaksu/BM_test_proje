apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # docker-compose ağında prometheus'un adresi
    url: http://prometheus:9090
    isDefault: true
    editable: false
